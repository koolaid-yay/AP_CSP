// AS 6th my family loop
#include <stdio.h>

int main(void){
    char family [6][20] = {"heidi (momma)", "robert (father)", "benny (brotato)", "rambo (dog)",
    "aimree (cus/sis)", "nammu (kitty)"};

    for(int i=6;)
}